{
  "_id": "_design/charseqs",
  "version": 3,
  "views": {
    "all": {
      "map": "function(doc) { 
        if (doc.category == 'charseq')  { emit(doc.name, doc.description) }
      }"
    }
  },
  "validate_doc_update": "{% include 'javascript_charseq_validation.dtl' %}"
}

{
   "_id": "_design/{{ _id }}",
   "version": 1,
   "views": {
       "alldocs": {
           "map": "function(doc) { if (doc.category == '{{ doctype }}' && doc.field_{{ _id }})  { emit(doc.field_{{ _id }}, doc) } }" 
       }
   }
}

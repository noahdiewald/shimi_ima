{
  "_id": "_design/doctypes",
  "version": 10,
  "views": {
    "all" : {
      "map": "function(doc) {
        if (doc.category === 'doctype') {
          emit(doc._id, doc.description);
        }                  
      }"
    }
  },
  "updates": {
    "stamp": "{% include 'javascript_stamp.dtl' %}"
  },
  "validate_doc_update": "{% include 'javascript_validation.dtl' %}",
  "lists": {
    "index_csv": "{% include 'javascript_index_csv.dtl' %}"
  }
}

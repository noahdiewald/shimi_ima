{% extends "field.dtl" %}
  
{% block input_area %}
  <select 
    class="field select ui-widget ui-corner-all"
    {% if default != "" %}
    data-field-default="{{ default|urlencode }}"
    {% endif %}
    {% include 'field_common_attributes.dtl' %}
  >
    {% if required != 'true' %}
    <option 
      value=""
      {% if default == '' %}selected=true{% endif %}
    ></option>
    {% endif %}
    {% for option in allowed %}
      <option
        {% if option|urlencode == default|urlencode %}selected=true{% endif %}
        value="{{ option|urlencode }}"
      >{{ option|force_escape }}</option>
    {% endfor %}
  </select>
{% endblock %}

{
   "_id": "_design/projects",
   "version": 1,
   "views": {
       "all": {
           "map": "function(doc) { if (doc.name && (doc.description != null))  emit(doc.name, doc.description) }"
       }
   }
}

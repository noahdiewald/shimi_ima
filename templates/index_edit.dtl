<div 
   id="index-editing-data" 
   data-index-doctype="{{doctype}}" 
   data-index-fields_label="{{fields_label|force_escape}}"
   data-index-fields="{{fields|force_escape}}"
   data-index-name="{{name}}"
   data-index-show_deleted="{{show_deleted}}"
   data-index-id="{{_id}}" 
   data-index-rev="{{_rev}}">
  <p>
    <em>{{name}}</em>
    will list
    <b>{{doctype}}</b>
    documents. They will be listed by 
    <b>{{label|join:", "}}</b>.
  </p>
</div>

<div id="index-conditions-listing">
<table>
  <thead>
    <tr class="ui-widget-header ui-corner-all">
      <td></td>
      <th>Negate</th>
      <th>Fieldset</th>
      <th>Field</th>
      <th>Condition</th>
      <th>Value</th>
      <td></td>
    </tr>
  </thead>
  <tbody>
  {% if rendered_conditions %}
    {{rendered_conditions}}
  {% endif %}
  </tbody>
</table>
</div>
